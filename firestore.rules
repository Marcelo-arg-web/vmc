rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // La app queda cerrada: solo usuarios logueados pueden leer o escribir.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
